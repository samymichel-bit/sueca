# TODO — SUECA (MVP → Premium)

## Étape 1 — Fix bug page Experts
- [ ] Mettre à jour `frontend/src/stores/main.js` : ajouter actions `fetchTeamMembers()` et `fetchTalents()`.
- [ ] Mettre à jour `frontend/src/views/Experts.vue` : sécuriser l’affichage (loading/empty state) sans casser l’existant.

## Étape 2 — Vérification globale
- [ ] Lancer `npm run build` dans `frontend/`.
- [ ] Lancer `python manage.py check` dans `backend/`.

